PORT=8787
# Postgres connection string for the Timeweb Cloud database.
# Percent-encode special characters in the password (e.g. > becomes %3E).
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=verify-full
